version: "2.1"

services:
  rabbitmq:
    image: rabbitmq:3.10.7-management
    ports:
      - 5672:5672
    environment:
      RABBITMQ_DEFAULT_USER: "admin"
      RABBITMQ_DEFAULT_PASS: "admin_password"
